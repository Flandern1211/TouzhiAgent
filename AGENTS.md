# TouzhiAgent 项目规则

## 项目边界

TouzhiAgent 是面向中国公募基金的个人研究辅助和风险监测系统。v1 的已确认目标、边界和验收草案以 [基金分析 Agent v1 需求规格](docs/requirements/fund-agent-v1-requirements.md) 为准；该文档在用户审查完成前仍是草案。

当前只允许搭建项目骨架。未获用户确认的产品行为、数据源、评分阈值、界面形态和外部服务，不得因为初始化而被当作已决定事项。

## 强制边界

- v1 使用 Python。
- 服务运行在用户本机；MySQL 等基础服务连接服务器实例。
- 基金和当前持仓由用户手动维护；v1 不实现交易流水、账户导入或自动对账。
- v1 覆盖多基金筛选、当前持仓快照分析、持续跟踪和系统内风险提醒。
- 数据可来自公开行情、官方公告、财经/行业新闻和社交媒体；默认简单爬虫，并支持配置外部爬虫 API。
- 固定规则发现异常，Agent 基于可追溯证据复核和解释。
- 风险提醒只在系统内展示；不得初始化邮件、飞书、微信、短信或手机推送。
- 不连接券商、不自动下单、不自动调仓。

## 文档规则

- 需求和验收写入 `docs/requirements/`，先审查再实现。
- `docs/project-structure.md` 描述当前骨架和边界。
- `docs/project-conventions.md` 保存稳定的编码、测试、安全和数据来源约定。
- `docs/coding/PRD.md` 作为需求文档索引；技术设计可以基于已明确的最小实现基线先形成草案，但必须标注待审查/待实现；UI 设计和 API 合同只有在相关需求或合同确认后才创建。
- 不把尚未确认的实现选择写成强制规则；如需新增范围，先更新需求文档并取得确认。

## 数据与安全规则

- 用于筛选、组合分析和风险提醒的重要事实必须保留来源、抓取时间和原文链接或等价引用。
- 明确区分正式净值、估算值、已披露事实、Agent 推断和未经证实的舆情线索。
- 数据库凭据和爬虫/模型 API 密钥不得写入代码库、文档或前端可见内容。
- 单个来源失败时必须保留失败状态，不得把无来源的旧数据伪装成最新结果。

## 验证与交付

- 骨架变更后运行 `D:\skills\bootstrap-project-governance\scripts\validate_project_docs.py` 指向本仓库根目录。
- 开始实现业务行为前，先让用户审查并确认需求文档；实现阶段再补充相应的技术设计和测试。
- 未经用户要求，不创建发布历史、部署脚本或外部集成配置。
